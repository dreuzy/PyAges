# Fast multi-chain installation smoke test.
#
# This profile exercises the complete CLI/YAML, pilot-covariance, diagnostics,
# serialization, and manifest path.  Its deliberately short chains are not a
# scientific convergence qualification; use one of the maintained extensive
# profiles documented in docs/user-guide/multichain-mh.md for that purpose.

workflow:
  kind: single_date

dataset:
  name: ploemeur_F09_2010.txt
  year: 2010
  data_dir: examples/natural/ploemeur/data
  verbose: false
  missing_error_rel: 0.01

lpm:
  model_name: exp_shifted
  data_directory: data_core/data_lpm

run:
  reachable_concentrations: false
  objective_function: false
  calibration_metropolis_hastings: true
  calibration_simplex: false

calibration_metropolis_hastings:
  nstep: 120
  burn_in: 0.1
  nskip: 10
  prior_option: false
  likelihood: true
  monitor: false
  display_traj: false
  multichain:
    enabled: true
    chains: 2
    master_seed: 20260831
    initialization:
      strategy: bounds_stratified
    pilot:
      enabled: true
      nstep: 40
      burn_in: 0.25
      relative_ridge: 1.0e-6
      proposal_multiplier: auto
      save_samples: true
    diagnostics:
      max_rhat: 1.01
      min_bulk_ess: 300
      min_tail_ess: 300
      require_convergence: false

results:
  study_name: smoke_multichain
