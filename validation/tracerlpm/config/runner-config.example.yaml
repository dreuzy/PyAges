# Copy this file to runner-config.local.yaml and adjust the paths.
# Git ignores the local file because it depends on the Windows installation.

# Absolute path to the qualified workbook copy; the file is never modified.
workbook_path: C:\CHEMIN\VERS\TracerLPM_V_1_0_Example1_fr_solver.xlsm
# Required SHA-256 of the workbook above; prevents use of another version.
workbook_sha256: 92F1D717AF519B9E0A701438BC99061D7633FD71C90A0657B787A0C563DFB179

# Absolute path to the native add-in for 64-bit Excel.
xll_path: C:\CHEMIN\VERS\TracerLPMfunctions_64_v_1.xll
# Required SHA-256 of the qualified XLL.
xll_sha256: 17C37328D606864754A2F115FEC1CA56A70A06FC05BC5123DEB1A462FC91F9A7

# Mapping of sheets, controls, macros, and ranges for this workbook hash.
workbook_map_path: C:\codes\pyages\validation\tracerlpm\config\workbook-map.yaml
# Isolated copies created for each simulation; Git ignores temporary content.
work_root: C:\codes\pyages\validation\tracerlpm\work
# JSON, CSV, and Markdown reports; Git ignores run output.
output_root: C:\codes\pyages\validation\tracerlpm\output

# Show Excel during computation. Keep true to diagnose dialog boxes.
excel_visible: true
# Maximum time, in seconds, allowed for a complete case recalculation.
timeout_seconds: 180
